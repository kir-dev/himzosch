<div class="w-full">

  <div class="flex justify-between items-center">
    <h1 class="font-bold text-4xl mb-5">Címkék</h1>
    <%= link_to 'Címke hozzáadása', new_label_path, class: "btn btn-secondary" %>
  </div>

  <div class="flex flex-row flex-wrap" style="flex-wrap: wrap;" >
    <% @labels.each do |label| %>
      <div class="m-1 justify-self-center">
        <%= render label %>
      </div>
    <% end %>
  </div>
</div>
